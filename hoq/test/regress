#!/bin/bash
#
#  Synopsis:
#	Very trivial regression test
#  Usage:
#	./regress
#
set -x
(cd ../src && make clean hoq) || exit

export LC_ALL=C
unset LANG LANGUAGE

PATH=.:../src:$PATH

seq 1000 | hoq regress.hoq | sort | shasum
cat regress.sha1
